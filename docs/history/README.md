# 迁移历史

这里保存“认知融接”建立前的认知资产。历史内容用于追溯，不自动拥有当前权威性。

- `legacy-dcf/`：从旧 `dcf-chatgpt-microcore` 远端 `main` 的权威快照迁入的 ADR 与项目文档。
- `collaboration-adr/`：从 `ai-collaboration-state` 迁入、与当前 DCF 架构直接相关的 ADR 演化流。
- `collaboration-project/`：迁入的旧 DCF 项目检查点。

迁移原则：保留原文、来源和历史状态；不静默把 proposed 改成 accepted，不把并发演化分支静默合并。
